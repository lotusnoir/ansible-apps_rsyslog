{{ ansible_managed | comment }}

# Dec 10 03:42:53 HOST  [a0c5e2] <passwd="*"> request denied by validnames option

if $programname == "nslcd" and ($msg contains "request denied by validnames option") then stop
